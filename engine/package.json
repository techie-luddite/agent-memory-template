{
  "name": "@agent-memory/template-engine",
  "version": "0.1.0",
  "description": "A template memory engine for agent continuity: staged capture, explicit promotion, bounded retrieval, and policy-aware lifecycle management.",
  "license": "Apache-2.0",
  "private": false,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/tests/smoke.test.js",
    "memory:validate": "node dist/src/cli.js validate",
    "memory:index": "node dist/src/cli.js index",
    "memory:retrieve": "node dist/src/cli.js retrieve"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "ajv": "^8.18.0",
    "ajv-formats": "^3.0.1"
  }
}
